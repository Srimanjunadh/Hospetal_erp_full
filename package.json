{
  "name": "medclues-erp-root",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt && npm install --prefix pms/frontend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "cd backend && ..\\.venv\\Scripts\\uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "pms:frontend": "npm run dev --prefix pms/frontend",
    "seed": "cd backend && ..\\.venv\\Scripts\\python seed.py",
    "dev": "npx concurrently \"npm run frontend\" \"npm run backend\"",
    "dev:all": "npx concurrently \"npm run frontend\" \"npm run backend\" \"npm run pms:frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix pms/frontend",
    "start": "npx concurrently \"npm run start --prefix frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
