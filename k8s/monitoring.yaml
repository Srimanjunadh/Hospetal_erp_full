apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: medclues-monitor
  namespace: medclues
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
  - port: metrics
    interval: 15s
    path: /metrics
